# Backend environment variables — copy to .env and fill in real values.
# This file IS committed. The actual .env is NOT committed (see .gitignore).

DB_URL=jdbc:mysql://localhost:3306/codechef_platform?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USERNAME=root
DB_PASSWORD=@Rohit8409

# Generate a strong secret: openssl rand -base64 64
JWT_SECRET=your_base64_encoded_jwt_secret_at_least_256_bits
